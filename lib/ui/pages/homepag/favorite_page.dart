import 'package:flutter/material.dart';

import '../../../theme.dart';

class FavPage extends StatelessWidget {
  const FavPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: kBg1,);
  }
}
